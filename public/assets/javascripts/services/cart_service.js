/*globals isttoApp, btoa*/
isttoApp.Services
  .service("CartService", ["lodash",
    "$http",
    "$q",
    "localStorageService", function (_, $http, $q, Storage) {

    return {
            
    };
  }]);
